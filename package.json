{
  "name": "claude-imaginepdf",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "node build/build.js",
    "clean": "rm -rf scripts/*.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0"
  }
}
